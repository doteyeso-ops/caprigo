# Caprigo box-mini

## Use
Double-click **RUN-CAPRIGO.cmd** → Caprigo TUI.

```
◆ Caprigo  Box  …
❯ your message
```

Slash: `/files` `/new` `/agents` `/quit`

Gateway auto-starts if down. Model: Ollama `qwen2.5:7b`.
