# Project Memory

- 2026-04-29 (runtime smoke): Verified current tree compiles (`npm run build`, `npm run build:web`) and the live gateway serves the built app bundle on `127.0.0.1:18789`. API smoke passed for offline flow: created an offline session, ran `gateway-ping`, and confirmed session trace/export includes `rationale`, `resultSummary`, `outputChars`, estimated token totals, pressure, and cost signal. Visual UI smoke was completed with a temporary local verification build and headless Edge screenshots, then reverted: Overview showed workflow library / recommended-next-move surfaces, and Board showed workflow recipes, selected-crew strip, linked worker state, and `Heavy` badge on a trace-loaded worker. Codex's own in-app browser path is still blocked here because its `node_repl` runtime is below the required Node version.
- 2026-04-29 (Phase 2 kickoff - workflow recipes MVP): Added Board-level `Workflow Recipes` for repeatable orchestrator launches. Recipes are local-first (`localStorage`), include built-in crew template selection, trigger metadata (`manual`, `file-change`, `daily-sweep` + condition text), and optional lead-instruction markdown injected into the orchestrator when launching. This remains intentionally lightweight and app-layer only.
- 2026-04-27 (polling optimization): Reduced background web polling pressure. Replaced the always-on `800ms` session poll with visibility-aware loops, slowed status refresh, and backed off the system/orchestration widgets when the tab is hidden. Goal is lower idle gateway noise and less laptop churn without making the UI feel stale.
- 2026-04-27 (Caprigo rename pass): Rebranded the product fully to Caprigo. Package scope is `@caprigo/*`, CLI/bin is `caprigo`, guide file is `CAPRIGO_LLM_GUIDE.md`, default data root is `~/.caprigo`, and public docs/UI copy now points to `caprigoai.com` as product home and `vibes-coded.com` as the marketplace. Removed the leftover compatibility shims from the abandoned pre-launch name.

- 2026-04-27 (doctor command): Added a Caprigo-native `doctor` command modeled after the upstream project’s diagnostics posture. It reports repo/config/permissions paths locally even when the gateway is down, and adds runtime/provider/model/skills/session details when Caprigo is reachable.
- 2026-04-27 (upstream merge pass 1): Borrowed the strongest practical idea from the upstream agent-runtime project we reviewed and adapted it to Caprigo Core: permission-hardened local tools. Added a Caprigo-native `permissions.json` model under the data root, scoped filesystem checks for file tools, and blocked-command + scoped-CWD enforcement for `execute_command`.
- 2026-04-27 (web first-run handoff): Added an Overview callout that appears when Caprigo is configured but no agents exist yet. It explicitly marks setup as complete and directs the user to create the first focused agent, closing the gap between setup and operation in the UI.
- 2026-04-27 (launcher polish): Added explicit launch/open control to guided setup, added a clearer completion banner in `setup.ps1`, and created `launch.ps1` / `launch.bat` as lightweight post-setup starters so users do not rerun the full installer path every time.
- 2026-04-27 (legacy data-root migration fix): Changed `caprigoDataRoot()` to always prefer `~/.caprigo` unless `CAPRIGO_HOME` is explicitly set. If only the legacy home exists, Caprigo now migrates it forward instead of continuing to report/use the old branded path. Verified locally with `caprigo doctor`: data root now resolves to `C:\Users\Laptop\.caprigo`.
- 2026-04-27 (repo-root setup wrappers): Added `setup.ps1` and `setup.bat` at the repo root so Windows users have one obvious install entry point. The PowerShell wrapper runs install, build, web build, and guided `caprigo setup --interactive`.
- 2026-04-27 (guided launch handoff): Extended `caprigo setup --interactive` so it can optionally start the gateway after writing config, wait for `/health`, and open the local Overview page. This reduces the last manual step between setup and first use.
- 2026-04-27 (interactive setup): Extended `caprigo setup` with an interactive mode that prompts for provider, detects models when reachable, and optionally writes the repo `.env`. This is the first concrete step toward making Caprigo feel installable instead of expecting manual config edits.
- 2026-04-27 (setup surface): Added a real `caprigo setup` CLI command for first-run diagnostics and turned the Overview first-launch list into a stateful checklist keyed off backend/model/skills/agent count. Goal is to make setup legible and recoverable before agents begin operating.
- 2026-04-27 (install path framing): Added `INSTALL_AND_FIRST_RUN.md` and `LANDING_PAGE_BRIEF.md` to make the beta story explicit: user setup first, agent operation second. Also updated CLI onboarding output and Overview/runtime copy so Caprigo itself teaches that boundary during first launch.
- 2026-04-27 (branding cleanup): Removed most Hermes wording from user-facing Caprigo docs, prompts, and UI copy. Kept Hermes references only where they still serve attribution or compatibility-documentation purposes, so beta users see Caprigo first instead of competitor framing.
- 2026-04-27 (compatibility subtree rename): Renamed the vendored skills subtree from `skills/agentskills/hermes/` to `skills/agentskills/imported-hermes/` and renamed the footer-maintenance script accordingly. This keeps upstream provenance explicit while making the repo layout read as imported compatibility content, not a product identity leak.
- 2026-04-27 (first-commit cleanup): Cleaned the extracted `C:\Users\Laptop\Desktop\New folder\Tools\Caprigo` snapshot back to git-ready state after smoke testing. Removed generated `.caprigo/` logs, removed `node_modules/`, dropped stale `skills/.radbot-skills-db.json`, ignored generated skill DB files in `.gitignore`, and synced small repo-facing doc/package cleanup from the source repo.
- 2026-04-27 (beta gate kit): Added `scripts/beta-smoke.ps1` plus `BETA_SMOKE_TEST.md`. The smoke kit covers gateway health, runtime/skills/offline catalog, orchestrator/worker/offline session creation, offline script execution, one direct LLM check, one soft orchestration-flow check, and Vibes marketplace reachability/install when listing credentials exist. Intended as the practical pre-beta launch gate.
- 2026-04-27 (launch audit pass): Tightened user-facing product language after the nav rename so beta users see `Overview`, `Board`, and `Session` more consistently. Added explicit `vibes-coded.com` marketplace-home language plus connector context to the global LLM guide, README, and `.env.example`. Current residuals are acceptable for beta: mostly internal variable names/comments and a few legacy docs with encoding artifacts, not runtime blockers.
- 2026-04-27 (global LLM primer): Added automatic workspace-level guide injection for all LLM sessions. New default file is `CAPRIGO_LLM_GUIDE.md` at the workspace root; env overrides: `CAPRIGO_LLM_GUIDE_PATH` or `CAPRIGO_PROGRAM_GUIDE_PATH`. The runtime now reads this guide before per-agent instructions so connected models understand Caprigo's UI, runtime modes, orchestration, and operator workflow before using the product.
- 2026-04-27 (repo extraction + nav cleanup): Renamed top-level web navigation labels toward a single workflow: `Overview`, `Board`, `Session`, `Settings`. Updated hero/runtime/session/board copy to reduce overlap between pages. Also created a clean standalone source-only folder at `C:\Users\Laptop\Desktop\New folder\Tools\Caprigo` for git-ready project files (excluded `.env`, `node_modules`, and `dist`).
- 2026-04-27 (beta UX pass): Improved the web app toward a cleaner beta story. Added builder starter templates (`Coder`, `Research`, `Fleet Coordinator`, `Local Script Runner`), clearer fleet summary chips, better empty-state guidance, and agent-card focus/activity summaries so objectives and readiness are more visible without opening details.
- 2026-04-27 (beta positioning): Reviewed Caprigo against current repo state. The product already has strong local-first primitives: per-agent instructions, offline scripts, orchestration, MCP, and pluggable backends. Main risk is not missing features but weak usability and focus. Beta should center on 2-3 user journeys, reduce UI/concept sprawl, make agent objective/status/action clearer, and resolve brand direction around the Caprigo name before launch materials harden.
- 2026-04-20 (OpenAI-compatible UX): Shared `openAICompatibleRequestHeaders` (Chrome-like UA; `CAPRIGO_OPENAI_USER_AGENT` override) for chat, `/health` probe, and `GET /api/openai/models`. Health returns `openai_probe_http_status` / `openai_probe_detail`. Web lists example bases (`openaiCompatibleBaseExamples.ts` + `OpenAiBaseExamplesList`) including `g4f` `/v1` and `/api/gemini`. Gateway chat error hints: env vars only for connectivity/401/403; `402` / balance / `PAYMENT_REQUIRED` -> credits/cheaper model hint. Chat-backend parses JSON `error.message` for cleaner errors.
- 2026-04-20: Rebranded to `Caprigo` (product), `Caprigo Core` (engine), `Caprigo CLI` (`caprigo`). npm scope `@caprigo/*`. Env: `caprigoEnv()` in `@caprigo/shared` merges legacy names. Data dir: `caprigoDataRoot()` -> `~/.caprigo` default with migration.
- 2026-04-19: Added a web `Desk` tab for saved multi-agent workflows. Desks persist in browser `localStorage` under Caprigo-prefixed keys.
- Desk workflow model: a desk has loaded agent session ids and ordered task steps. Each step targets one agent and posts to the existing `/api/sessions/:id/messages` endpoint.
- Chaining convention: use `{{previous}}` in a later step prompt to inject the previous agent response.
- Keep in mind: workflow persistence is currently browser-local, not gateway-backed. Good next server step would be `/api/desks` if desks need to follow the machine/user across browsers.
- 2026-04-27 (website v1): Created a standalone Next.js marketing site in `C:\Users\Laptop\Desktop\New folder\Tools\Caprigo - Website` for `caprigoai.com`. It is a one-page SEO-focused landing site with install steps, local-first positioning, Vibes marketplace framing, metadata routes, JSON-LD, OG image generation, and Railway-ready config.
- 2026-04-27 (website stats): Added a stats strip plus `/api/stats` to the Caprigo website. Page views can persist through `DATABASE_URL` on Railway Postgres; install/download counts are seeded with `NEXT_PUBLIC_INSTALL_COUNT`, `NEXT_PUBLIC_DOWNLOAD_COUNT`, and optional `NEXT_PUBLIC_SITE_VIEWS` when no DB is attached.
- 2026-04-27 (website git): Created a standalone git repo for `C:\Users\Laptop\Desktop\New folder\Tools\Caprigo - Website`, committed the site, removed a stray local backup zip, and created the private GitHub remote `https://github.com/doteyeso-ops/caprigo-website` for Railway deployment.
- 2026-04-27 (website counters + Railway): Reworked the Caprigo website counters so traffic uses runtime API reads instead of build-time DB access, then provisioned Railway Postgres for the `caprigo-website` service. Live `/api/stats` now reports `dataSource: "database"`, and setup/download counters are click-based metrics rather than static placeholders.
- 2026-04-28 (CLI noise + polish): Defaulted gateway request logging to `smart` mode so routine browser polls do not flood stdout, slowed the most aggressive Overview polling loops again, and refreshed the CLI/launch header around the "Capricorn x Virgo" identity so startup feels intentional instead of raw.
- 2026-04-28 (token-efficiency pass 1): Added hard caps for global guide injection, per-session markdown instruction files, and inline instructions. Prompt assembly now compresses older conversation into a digest after a recent-message window, and tool results are truncated before being fed back into the next LLM step. Added env knobs in `.env.example` and documented them in `README.md`.
- 2026-04-28 (repo-aware context pass 1): Added built-in `repo_map` skill to generate a compact local codebase structure map with file-relative symbol listings. This is the first step toward codebase-aware context selection without repeatedly injecting full files into prompts.
- 2026-04-28 (repo-aware context pass 2): Added built-in `codebase_context` skill that combines repo mapping and text search into one compact retrieval call. Goal is to make repo-aware context the normal path before full-file reads, especially for coding/debugging tasks.
- 2026-04-28 (repo-aware context pass 3): Added lightweight relevance scoring to `codebase_context`. Candidate files are now ranked with explicit reasons such as direct text hits, path-token matches, and symbol matches. This stays within roadmap scope: better retrieval quality without committing to a full indexing/tree-sitter subsystem yet.
- 2026-04-28 (trace visibility pass 1): Extended execution-log entries with compact result summaries and output-size proxies, added per-session `/api/sessions/:id/execution-log`, and surfaced a recent trace panel in the agent details dialog. This gives users a lightweight replay/cost view for retrieval and tool use without building a separate observability product yet.
- 2026-04-29 (repo-aware context pass 4): Upgraded repo mapping for TS/JS-family files from pure regex extraction to AST-backed extraction via the TypeScript parser. `repo_map` / `codebase_context` now identify classes, interfaces, types, enums, functions, methods, and function-valued properties more reliably for the repo’s main code paths while keeping the older heuristic fallback for other languages.
- 2026-04-29 (fleet UX pass 1): Added first Board orchestration polish and starter crew launchers. Board now exposes direct role conversion plus attach/detach flows in the context menu, shows worker/reporting status on cards, and can launch two starter crews: `Repo Crew` and `Automation Crew`. This stays frontend/app-layer only for now; no dedicated template backend yet.
- 2026-04-29 (fleet UX pass 2): Tightened the Board interactions around those crews. Crew launches now show a short in-app summary banner, converting an orchestrator with attached workers is blocked until the crew is detached, promoting a worker that already reports upward now asks for confirmation, and cards highlight the currently selected crew relationship more clearly without changing the backend model.
- 2026-04-29 (MCP polish pass 1): Upgraded Settings -> MCP tools from raw-form-first to recommendation-first. Added curated one-click presets for the official filesystem MCP server, official GitHub MCP server, and Windows-MCP on Windows hosts. Presets still land in the existing `/api/mcp-servers` flow, so this improves discovery/install UX without adding a new persistence model.
- 2026-04-29 (trace visibility pass 2): Extended the per-session trace view with compact `why` summaries for tool calls and added export endpoints/UI for markdown and JSON trace bundles. This keeps replay cheap and inspectable, and it does not blur the marketplace story: Vibes-Coded remains the marketplace while MCP stays an integration surface.
- 2026-04-29 (resource dashboard pass 1): Added a lightweight Overview fleet-resource card. It combines live system pressure from `/api/system-monitor` with fleet composition counts so users can see CPU/memory pressure, active thinking agents, offline agents, orchestrators, linked workers, and current gateway RSS without opening the full monitor panel.
- 2026-04-29 (Vibes-Coded polish pass 1): Improved the Overview marketplace surface without changing backend APIs. Added quick-search chips, marketplace readiness/import summary, clearer install result framing, and a compact list of installed Vibes-Coded tools so the marketplace path feels intentional and stays distinct from MCP integrations.
- 2026-04-29 (Overview workflow pass 1): Added a `Recommended next move` card to Overview that turns current runtime/fleet/marketplace state into direct actions: open Settings, create the first agent, launch a starter crew, or search Vibes-Coded. This makes setup, crews, and marketplace imports feel like one guided path instead of separate concepts.
- 2026-04-29 (handoff polish pass 1): Tightened the Overview -> Session -> Board flow without adding backend APIs. Overview fleet cards now expose direct `Open session` / `Open board` actions plus quicker details access, and Session now includes quick actions back to Board/details with a small crew-status hint for orchestrators and linked workers.
- 2026-04-29 (Board crew grouping pass 1): Added lightweight crew shells behind orchestrator-led groups on the Board canvas. Linked workers now read as one crew with a labeled boundary around them, using existing positions and links only, so the fleet feels more deliberate without adding layout persistence or new orchestration data.
- 2026-04-29 (Board crew actions pass 1): Added a selected-crew strip above the Board canvas. When a lead or worker in a crew is selected, the Board now surfaces the lead, member list, and direct actions to open the lead session or inspect lead details, so users can move around a crew from one place instead of hunting through the canvas.
- 2026-04-29 (starter crew guidance pass 1): Upgraded the temporary post-launch Board notice into a short crew brief. `Repo Crew` and `Automation Crew` now launch with a clearer “start with the lead” message, a small first-task checklist, and a one-click `Open lead session` action so the first move is explicit instead of implied.
- 2026-04-29 (starter crew guidance pass 2): The crew-launch `Open lead session` action now pre-fills a strong first-task draft in Session for `Repo Crew` and `Automation Crew`. This keeps the built-in crews lightweight but removes the blank-textbox problem right after launch.
- 2026-04-29 (trace summary pass 1): Extended the session trace UI with cheap summary signals derived from existing entries: trace-pressure level (`light` / `watch` / `heavy`), average call duration, average output chars, slowest tool, noisiest tool, and latest failure. No new backend API was needed for this pass.
- 2026-04-29 (workflow template pass 1): Added `Launch Audit Crew` as a third built-in orchestrator-led workflow. It appears in Overview and Board launch surfaces, creates `Launch Audit Lead` + `Surface Checker` + `Risk Reviewer`, and launches with a lead-first brief plus a prefilled audit prompt draft in Session.
- 2026-04-29 (workflow template pass 2): Added `PR Review Crew` as another packaged workflow. It creates `PR Review Lead` + `Diff Scout` + `Risk Reviewer`, appears in Overview and Board launch surfaces, and launches with a review-specific lead brief plus a prefilled prompt draft for local diff / patch / PR review work.
- 2026-04-29 (workflow library pass 1): Added a lightweight Overview `Workflow library` card that lists the built-in crews with roles, best-fit use cases, and one-click launch actions. This makes the packaged workflows feel like a curated product layer instead of scattered buttons.
- 2026-04-29 (workflow library pass 2): Added a shared workflow launcher dialog plus a single workflow-library data source for the built-in crews. Overview and Board can now open the same picker, so workflow launch is no longer duplicated across scattered buttons.
- 2026-04-29 (workflow metadata pass 1): Added lightweight workflow identity in Session and agent details by inferring built-in crew context from lead/member naming. Launched workflow crews now explain what they are for after creation, without adding backend workflow state.
- 2026-04-29 (trace estimate pass 1): Added rough context/output token estimates and a cost-signal badge to the trace summary UI. This is intentionally heuristic and frontend-only, derived from trace text volume rather than real provider billing data.
- 2026-04-29 (trace warning pass 1): Added per-session heavy-trace warnings in Session and Board using a shared trace heuristic helper. Sessions with high recent context/cost pressure now show an inline warning in Session and a `Heavy` badge on Board cards.
- 2026-04-29 (trace estimate pass 2): Moved rough trace estimates into the gateway session-summary/export path. `/api/sessions/:id/execution-log` totals and markdown/JSON trace export now include estimated context tokens, output tokens, total tokens, pressure, and cost signal so replay artifacts carry the same heuristics as the live UI.
